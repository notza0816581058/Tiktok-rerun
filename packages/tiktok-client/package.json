{
  "name": "@live-hub/tiktok-client",
  "version": "0.2.0",
  "private": true,
  "description": "Typed TikTok integration boundary with deterministic mock transport",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
